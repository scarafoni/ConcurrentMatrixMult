module Main where
import System.Environment
import GHC.TypeLits
import Numeric.LinearAlgebra.HMatrix at LA

main :: IO ()
main = do
    putStrLn("done")

add :: Int -> Int -> Int
add x y = 
    x + y

